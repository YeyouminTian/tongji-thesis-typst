#import "../lib.typ": chapter, fonts

#chapter(label: <ch:text>)[文字排版][

本章说明正文的字体、字号、行距与常见强调方式，并演示脚注与行内交叉引用。

== 正文与字体

正文采用宋体小四号，固定行距 20 磅，段前段后为 0，首行缩进两个汉字符，并采用两端对齐。英文与数字使用 Times New Roman，例如 The quick brown fox jumps over 123 units。中英文混排时字距由模板统一控制，无需手工调整。

Typst 以“固定行高 = 字号 + 行距（leading）”换算 Word 的固定行距。正文小四号、20 磅行高对应 leading 为 8 磅，相关取值集中定义在 `utils/typography.typ`，改动字号或行距只需修改该处，全文随之统一生效。

== 强调与引用

行文中可用多种方式强调：*加粗* 用于突出关键结论；#text(font: fonts.kai)[楷体] 常用于中文语境下的着重或引述；行内代码 `#figure(...)` 用于呈现命令、函数或文件名。三者可按语义混用，但同一篇论文中应保持一致。

较长的引文可用块引用另起，与正文区隔：

#quote(block: true, attribution: [排版格式说明])[
  图、表、公式的编号与交叉引用应由系统自动维护，作者只需为其命名并在正文中引用，避免因增删内容而手工改号。
]

== 脚注与交叉引用

脚注适合补充说明或标注出处，正文标注后会自动排至页脚#footnote[脚注随正文自动编号，并排版于当前页面底部，不占用正文行距。]。交叉引用则通过标签自动关联目标：为目标命名后，正文用 `@` 加标签即可自动生成编号，编号变动无需手工维护。图、表、公式各自的引用写法，将在第 3 章、第 4 章结合实例演示。
]
