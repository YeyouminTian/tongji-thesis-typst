#import "../lib.typ": chapter

#chapter(label: <ch:intro>)[绪论][

本模板用于排版同济大学研究生学位论文，覆盖封面、中英文摘要、目录、符号说明、正文、参考文献、附录、致谢与个人简历等完整结构。本章介绍模板的定位、编译方式与内容组织，并借助本章自身演示多级标题、列表与章节交叉引用等基础排版能力。

== 模板简介

本模板基于排版系统 Typst 构建，以纯文本源码描述论文的内容与格式，配合版本管理即可完成协作写作。与传统字处理软件相比，其排版规则集中定义、可编程复用：改动一处即可全局生效，特别适合结构稳定、篇幅较大且需长期维护的学位论文。

正文默认采用宋体小四号、固定 20 磅行距，英文使用 Times New Roman；图、表、公式按章自动编号，参考文献遵循 GB/T 7714 数字著录格式。作者通常只需专注内容本身，版面格式由模板统一保证。

== 快速开始

=== 环境准备

首先安装 Typst 与论文所需字体（中文宋体、黑体、楷体，英文 Times New Roman、Arial）。字体缺失时 Typst 会自动回退到近似字体，但正式定稿建议补齐字体，以获得一致的版面效果。

=== 编译整篇论文

在仓库根目录执行下列任一方式，即可生成完整 PDF：

+ 运行脚本 `./scripts/build.sh`；
+ 直接调用 `typst compile thesis.typ thesis.pdf`。

=== 单章预览

写作阶段逐章调试时无需每次编译整篇。执行 `./scripts/compile-chapter.sh chapter1 chapter1.pdf` 即可只编译单章，显著缩短预览等待。

== 文档结构与分卷

全文由入口文件 `thesis.typ` 组装，分为三卷：

/ 前置部分（frontmatter）: 摘要、目录、符号说明，页码采用罗马数字；
/ 主体部分（mainmatter）: 各章正文，页码从 1 重新计数；
/ 后置部分（backmatter）: 参考文献、附录、致谢与个人简历。

三卷分别通过 `#frontmatter[...]`、`#mainmatter[...]`、`#backmatter[...]` 切换，页眉、页码与标题样式随之自动调整。

=== 新增章节

新增一章只需三步：

+ 在 `chapters/` 下新建 `chapterN.typ`；
+ 文件开头写 `#import "../lib.typ": chapter`，随后以 `#chapter[章标题][ …… ]` 组织内容；
+ 在 `thesis.typ` 的 `#mainmatter[...]` 中加入 `#include "chapters/chapterN.typ"`。

本模板其余各章分别演示不同排版要素：文字排版见第 2 章，图、表与可视化见第 3 章，公式、文献与其它学术要素见第 4 章。为章、图、表、公式或附录加上标签后，正文即可用 `@` 加标签自动引用（例如 `@ch:标签` 显示为“第 X 章”），编号变动时无需手工维护。

== 标题与列表

本模板支持四级标题：章标题（如“第 1 章”）为一级，`==`、`===`、`====` 分别对应二至四级，其中四级标题以“(1)”样式编号。本节及其下属条目即在逐级演示。

=== 三级标题示例

三级标题用于对二级小节进一步细分，编号形如“1.5.1”。

==== 四级标题示例

四级标题编号形如“(1)”，通常用于列举并列要点，其下一般直接承接正文，不再细分。

无序列表与有序列表可自由嵌套：

- 数据准备
  - 影像与边界
  - 属性与文本
- 特征提取
- 结果综合

术语列表则适合解释成对出现的名词，例如：

/ Typst: 一种面向排版的标记语言与编译器；
/ 交叉引用: 通过标签自动关联图、表、公式、章节与文献的机制，编号变动时无需手工维护。
]
